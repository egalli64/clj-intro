;; clj-intro - Introduction to Clojure
;; https://github.com/egalli64/clj-intro
;;
;; For more info read Getting Clojure by Russ Olsen
;; def: bind a symbol to a value

;; use kebab-case for symbols
(def first-name "Russ")
(println first-name)

;; the value generated to the expression is bound to the symbol
(def the-average (/ (+ 20 40.0) 2))
(println the-average)
