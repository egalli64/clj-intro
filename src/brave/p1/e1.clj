;;; Running a Clojure script
;;; From CLI, top folder in the repository, assuming src as source root: clj -M src\brave\p1\e1.clj
;; -M tells Clojure _not_ to start the REPL to run the script
;;
;; to start just the REPL: clj
;; Ctrl+C to terminate it

(println "Hello, World!")