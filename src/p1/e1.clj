;;; Running a Clojure script
;;; From CLI, top folder in the repository, assuming src as source root: clj -M src\p1\e1.clj
;; -M tells Clojure _not_ to start the REPL to run the script

(println "Hello, World!")